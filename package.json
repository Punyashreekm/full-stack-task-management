{
    "name": "taskflow-root",
    "version": "1.0.0",
    "scripts": {
        "install-all": "npm install && cd backend && npm install && cd ../vite-project && npm install",
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix vite-project\"",
        "start:backend": "npm run dev --prefix backend",
        "start:frontend": "npm run dev --prefix vite-project"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}